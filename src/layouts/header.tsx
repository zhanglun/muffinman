import { Input } from "@heroui/react";

export const Header = () => {
  return (
    <div>
      <span>MuffinMan</span>
      <span>Menu</span>
      <Input placeholder="Search" />
    </div>
  );
};
